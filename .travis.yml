matrix:
  include:
    - os: linux
      language: node_js
      node_js: '12'

cache:
  directories:
    - node_modules

script:
  - npm ci
  - CI=true npm test --passWithNoTests --ci
  - npm build

branches:
  except:
    - "/^v\\d+\\.\\d+\\.\\d+$/"
